# 🛒 Django Ninja Магазин

Простой API магазина на Django Ninja

## 🚀 Быстрый старт

```bash
# Активировать окружение
source .venv/bin/activate

# Запустить сервер
python manage.py runserver
```

## 📋 Что доступно?

### 1. **Интерактивная документация API**
Открой в браузере: http://127.0.0.1:8000/api/docs

Здесь можно тестировать все запросы прямо в браузере!

### 2. **Админка Django**
http://127.0.0.1:8000/admin/
- Логин: `admin`
- Пароль: (нужно установить через команду ниже)

```bash
python manage.py changepassword admin
```

## 🔥 API Endpoints

### Категории
- `GET /api/shop/categories` - список всех категорий
- `POST /api/shop/categories` - создать категорию
- `GET /api/shop/categories/{id}` - получить категорию

### Товары
- `GET /api/shop/products` - список товаров
- `GET /api/shop/products?category_id=1` - фильтр по категории
- `GET /api/shop/products?in_stock=true` - только в наличии
- `POST /api/shop/products` - создать товар
- `GET /api/shop/products/{id}` - получить товар
- `DELETE /api/shop/products/{id}` - удалить товар

## 💡 Примеры запросов

### Создать категорию (POST)
```json
{
  "name": "Спорт",
  "description": "Спортивные товары"
}
```

### Создать товар (POST)
```json
{
  "name": "MacBook Pro",
  "description": "Ноутбук для разработки",
  "price": 2499.99,
  "category_id": 1,
  "in_stock": true
}
```

## 📁 Структура проекта

```
shop/
├── models.py       # Модели базы данных (Category, Product)
├── schemas.py      # Схемы для валидации API
├── api.py          # Все endpoints API
└── admin.py        # Настройка админки

shop_project/
├── settings.py     # Настройки Django
└── urls.py         # Маршруты (подключение API)
```

## 🎯 Что дальше?

1. **Добавить корзину** - модель Cart и CartItem
2. **Добавить заказы** - модель Order
3. **Авторизацию** - регистрация/вход пользователей
4. **Загрузку картинок** - ImageField для Product

## 🛠 Полезные команды

```bash
# Создать миграции после изменения models.py
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперюзера
python manage.py createsuperuser

# Запустить тесты
python manage.py test
```
